// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: activation.sql

package db

import (
	"context"
)

const dismissActivationGuide = `-- name: DismissActivationGuide :exec
UPDATE users
SET activation_guide_dismissed_at = NOW(), updated_at = NOW()
WHERE id = $1 AND activation_guide_dismissed_at IS NULL
`

func (q *Queries) DismissActivationGuide(ctx context.Context, id string) error {
	_, err := q.db.Exec(ctx, dismissActivationGuide, id)
	return err
}

const getUserActivationCounts = `-- name: GetUserActivationCounts :one
SELECT
  (SELECT COUNT(*)::INTEGER
    FROM social_accounts sa
    JOIN profiles p ON p.id = sa.profile_id
    JOIN workspaces w ON w.id = p.workspace_id
    WHERE w.user_id = $1
      AND sa.disconnected_at IS NULL
      AND sa.status = 'active') AS connected_accounts_count,
  (SELECT COUNT(*)::INTEGER
    FROM social_posts sp
    JOIN workspaces w ON w.id = sp.workspace_id
    WHERE w.user_id = $1
      AND sp.status = 'published') AS posts_sent_count,
  (SELECT COUNT(*)::INTEGER
    FROM api_keys ak
    JOIN workspaces w ON w.id = ak.workspace_id
    WHERE w.user_id = $1
      AND ak.revoked_at IS NULL) AS api_keys_count
`

type GetUserActivationCountsRow struct {
	ConnectedAccountsCount int32 `json:"connected_accounts_count"`
	PostsSentCount         int32 `json:"posts_sent_count"`
	ApiKeysCount           int32 `json:"api_keys_count"`
}

// Returns the three activation-guide counters for a user by aggregating
// across all their workspaces. Step completion is derived from these on
// the handler side — we never store per-step state because reality is
// the source of truth (if a user deletes their only account, step 1
// correctly reverts to incomplete).
func (q *Queries) GetUserActivationCounts(ctx context.Context, userID string) (GetUserActivationCountsRow, error) {
	row := q.db.QueryRow(ctx, getUserActivationCounts, userID)
	var i GetUserActivationCountsRow
	err := row.Scan(&i.ConnectedAccountsCount, &i.PostsSentCount, &i.ApiKeysCount)
	return i, err
}

const markActivationCompleted = `-- name: MarkActivationCompleted :exec
UPDATE users
SET activation_completed_at = NOW(), updated_at = NOW()
WHERE id = $1 AND activation_completed_at IS NULL
`

func (q *Queries) MarkActivationCompleted(ctx context.Context, id string) error {
	_, err := q.db.Exec(ctx, markActivationCompleted, id)
	return err
}
