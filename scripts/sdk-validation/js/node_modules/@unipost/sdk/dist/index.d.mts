interface HttpClientOptions {
    apiKey: string;
    baseUrl: string;
    timeout: number;
}
declare class HttpClient {
    private readonly apiKey;
    private readonly baseUrl;
    private readonly timeout;
    constructor(options: HttpClientOptions);
    request<T>(method: string, path: string, options?: {
        body?: unknown;
        query?: Record<string, string | number | undefined>;
        headers?: Record<string, string>;
    }): Promise<T>;
    get<T>(path: string, query?: Record<string, string | number | undefined>): Promise<T>;
    post<T>(path: string, body?: unknown, headers?: Record<string, string>): Promise<T>;
    put<T>(path: string, body?: unknown): Promise<T>;
    delete<T>(path: string): Promise<T>;
}

type Platform = "twitter" | "linkedin" | "instagram" | "threads" | "tiktok" | "youtube" | "bluesky";
type AccountStatus = "active" | "reconnect_required" | "disconnected";
type ConnectionType = "byo" | "managed";
interface SocialAccount {
    id: string;
    platform: Platform;
    account_name: string | null;
    external_user_id?: string;
    external_user_email?: string;
    connected_at: string;
    status: AccountStatus;
    connection_type: ConnectionType;
}
interface AccountHealth {
    account_id: string;
    status: "ok" | "degraded" | "disconnected";
    last_checked_at: string;
    error?: string;
}
interface ListAccountsParams {
    platform?: Platform;
    externalUserId?: string;
    status?: AccountStatus;
}

type PostStatus = "draft" | "scheduled" | "processing" | "published" | "partial" | "failed" | "cancelled";
interface Post {
    id: string;
    caption: string | null;
    status: PostStatus;
    scheduled_at?: string;
    created_at: string;
    published_at?: string;
    results?: PlatformResult[];
}
interface PlatformResult {
    social_account_id: string;
    platform?: Platform;
    account_name?: string;
    status: string;
    external_id?: string;
    error_message?: string;
    published_at?: string;
}
interface CreatePostPlatformPost {
    accountId: string;
    caption?: string;
    threadPosition?: number;
    firstComment?: string;
    mediaIds?: string[];
}
interface CreatePostParams {
    caption?: string;
    accountIds?: string[];
    platformPosts?: CreatePostPlatformPost[];
    mediaUrls?: string[];
    scheduledAt?: string;
    status?: "draft";
    idempotencyKey?: string;
}
interface ListPostsParams {
    status?: PostStatus;
    platform?: Platform;
    from?: string;
    to?: string;
    limit?: number;
    cursor?: string;
}
interface PostAnalytics {
    post_id: string;
    impressions: number;
    engagements: number;
    likes: number;
    comments: number;
    shares: number;
    clicks: number;
    results: Record<string, Record<string, number>>;
}

type WebhookEventType = "post.published" | "post.failed" | "post.scheduled" | "post.cancelled" | "account.connected" | "account.disconnected" | "account.reconnect_required";
interface WebhookEvent {
    id: string;
    event: WebhookEventType;
    created_at: string;
    data: Record<string, unknown>;
}
interface VerifyWebhookOptions {
    payload: string | Buffer;
    signature: string;
    secret: string;
}

interface ConnectSession {
    id: string;
    url: string;
    status: "pending" | "completed" | "expired";
    expires_at: string;
    platform: string;
    external_user_id: string;
}
interface CreateConnectSessionParams {
    platform: string;
    externalUserId: string;
    externalUserEmail?: string;
    returnUrl: string;
}
interface ManagedUser {
    external_user_id: string;
    external_user_email?: string;
    accounts: SocialAccount[];
    created_at: string;
}
interface MediaUploadRequest {
    filename: string;
    contentType: string;
    sizeBytes: number;
}
interface MediaUploadResponse {
    mediaId: string;
    uploadUrl: string;
}
type Granularity = "day" | "week" | "month";
type GroupBy = "platform" | "social_account_id" | "status";
interface AnalyticsRollupParams {
    from: string;
    to: string;
    granularity?: Granularity;
    groupBy?: GroupBy;
}
interface AnalyticsRollup {
    from: string;
    to: string;
    granularity: Granularity;
    buckets: AnalyticsBucket[];
}
interface AnalyticsBucket {
    key: string;
    impressions: number;
    engagements: number;
    likes: number;
    comments: number;
    shares: number;
    clicks: number;
}
interface PaginatedResponse<T> {
    data: T[];
    nextCursor?: string;
    meta?: {
        total: number;
        page: number;
        per_page: number;
    };
}
interface UniPostClientOptions {
    apiKey?: string;
    baseUrl?: string;
    timeout?: number;
}

declare class Accounts {
    private readonly http;
    constructor(http: HttpClient);
    /** List all connected social accounts. */
    list(params?: ListAccountsParams): Promise<PaginatedResponse<SocialAccount>>;
    /** Get a single account by ID. */
    get(accountId: string): Promise<SocialAccount>;
    /** Check account health status. */
    health(accountId: string): Promise<AccountHealth>;
}

declare class Posts {
    private readonly http;
    constructor(http: HttpClient);
    /** Create a new post. */
    create(params: CreatePostParams): Promise<Post>;
    /** List posts with optional filters and cursor pagination. */
    list(params?: ListPostsParams): Promise<PaginatedResponse<Post> & {
        nextCursor?: string;
    }>;
    /** Iterate all posts using async generator (auto-pagination). */
    listAll(params?: Omit<ListPostsParams, "cursor">): AsyncGenerator<Post>;
    /** Get a single post by ID. */
    get(postId: string): Promise<Post>;
    /** Get analytics for a post. */
    analytics(postId: string): Promise<PostAnalytics>;
    /** Publish a draft post. */
    publish(postId: string): Promise<Post>;
    /** Cancel a scheduled post. */
    cancel(postId: string): Promise<Post>;
    /** Bulk create posts (up to 50). */
    bulkCreate(posts: CreatePostParams[]): Promise<Post[]>;
}

declare class Media {
    private readonly http;
    constructor(http: HttpClient);
    /** Request a presigned upload URL. */
    upload(params: MediaUploadRequest): Promise<MediaUploadResponse>;
    /**
     * Convenience: upload a local file (Node.js only).
     * Reads the file, requests a presigned URL, uploads, and returns the mediaId.
     */
    uploadFile(filePath: string): Promise<string>;
}

declare class Analytics {
    private readonly http;
    constructor(http: HttpClient);
    /** Get aggregated analytics rollup. */
    rollup(params: AnalyticsRollupParams): Promise<AnalyticsRollup>;
}

declare class Connect {
    private readonly http;
    constructor(http: HttpClient);
    /** Create a Connect session for end-user OAuth. */
    createSession(params: CreateConnectSessionParams): Promise<ConnectSession>;
    /** Get the status of a Connect session. */
    getSession(sessionId: string): Promise<ConnectSession>;
}

declare class Users {
    private readonly http;
    constructor(http: HttpClient);
    /** List all managed users (Connect). */
    list(): Promise<PaginatedResponse<ManagedUser>>;
    /** Get a single managed user by external_user_id. */
    get(externalUserId: string): Promise<ManagedUser>;
}

/**
 * Official UniPost API client.
 *
 * @example
 * ```ts
 * import { UniPost } from '@unipost/sdk'
 *
 * const client = new UniPost() // reads UNIPOST_API_KEY env var
 *
 * const post = await client.posts.create({
 *   caption: 'Hello from UniPost!',
 *   accountIds: ['sa_twitter_xxx'],
 * })
 * ```
 */
declare class UniPost {
    readonly accounts: Accounts;
    readonly posts: Posts;
    readonly media: Media;
    readonly analytics: Analytics;
    readonly connect: Connect;
    readonly users: Users;
    constructor(options?: UniPostClientOptions);
}

/**
 * Base error class for all UniPost API errors.
 */
declare class UniPostError extends Error {
    readonly status: number;
    readonly code: string;
    constructor(message: string, status: number, code: string);
}
/** 401 - API key invalid or expired. */
declare class AuthError extends UniPostError {
    constructor(message?: string);
}
/** 404 - Resource not found. */
declare class NotFoundError extends UniPostError {
    constructor(message?: string);
}
/** 422 - Validation error. */
declare class ValidationError extends UniPostError {
    readonly errors: Record<string, string[]>;
    constructor(message?: string, errors?: Record<string, string[]>);
}
/** 429 - Rate limit exceeded. */
declare class RateLimitError extends UniPostError {
    readonly retryAfter: number;
    constructor(retryAfter: number, message?: string);
}
/** Platform-side error (e.g. Twitter rejected the post). */
declare class PlatformError extends UniPostError {
    readonly platform: string;
    constructor(message: string, platform: string);
}
/** Monthly quota exceeded. */
declare class QuotaError extends UniPostError {
    constructor(message?: string);
}

/**
 * Verify the signature of a UniPost webhook request.
 *
 * Uses HMAC-SHA256 with the webhook secret to validate the payload.
 */
declare function verifyWebhookSignature(options: VerifyWebhookOptions): Promise<boolean>;

export { type AccountHealth, type AccountStatus, type AnalyticsBucket, type AnalyticsRollup, type AnalyticsRollupParams, AuthError, type ConnectSession, type ConnectionType, type CreateConnectSessionParams, type CreatePostParams, type CreatePostPlatformPost, type Granularity, type GroupBy, type ListAccountsParams, type ListPostsParams, type ManagedUser, type MediaUploadRequest, type MediaUploadResponse, NotFoundError, type PaginatedResponse, type Platform, PlatformError, type PlatformResult, type Post, type PostAnalytics, type PostStatus, QuotaError, RateLimitError, type SocialAccount, UniPost, type UniPostClientOptions, UniPostError, ValidationError, type VerifyWebhookOptions, type WebhookEvent, type WebhookEventType, verifyWebhookSignature };
